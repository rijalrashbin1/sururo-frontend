# Frontend Environment Variables
# Copy to .env and adjust as needed

# When running locally with vite proxy, leave blank — requests go to /api and get proxied to localhost:5000
VITE_API_URL=

# For production (deployed frontend pointing at deployed backend)
# VITE_API_URL=https://your-backend.onrender.com/api
